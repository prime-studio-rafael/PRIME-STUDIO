// Espelha server/utils/additionalInstruction.js — mesma constante, nunca dois números
// diferentes entre backend e frontend para o mesmo limite.
export const ADDITIONAL_INSTRUCTION_MAX_LENGTH = 500;
