import { AlertCircle, ImageIcon } from 'lucide-react';
import { useMemo, useState } from 'react';
import ResultCard from './ResultCard.jsx';
import ResultDetailModal from './ResultDetailModal.jsx';
const FILTERS = [['all', 'Todos'], ['pending', 'Aguardando aprovação'], ['approved', 'Aprovados'], ['rejected', 'Reprovados']];
export default function ResultsPage({ history }) {
  const [filter, setFilter] = useState('all'); const [deleteTarget, setDeleteTarget] = useState(null);
  const filtered = useMemo(() => filter === 'all' ? history.results : history.results.filter((result) => result.reviewStatus === filter), [filter, history.results]);
  return <><header className="mb-8 border-b border-slate-200/80 pb-6"><div className="mb-3 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.18em] text-slate-500"><ImageIcon size={14} /> PRIME IA STUDIO</div><h1 className="text-3xl font-semibold tracking-[-0.035em] text-slate-950">Resultados</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Consulte, compare e avalie as gerações preservadas neste computador.</p></header><div className="mb-6 flex flex-wrap gap-2" aria-label="Filtros de resultados">{FILTERS.map(([value, label]) => <button key={value} type="button" onClick={() => setFilter(value)} aria-pressed={filter === value} className={`rounded-full border px-3.5 py-2 text-xs font-semibold ${filter === value ? 'border-slate-950 bg-slate-950 text-white' : 'border-slate-200 bg-white text-slate-600'}`}>{label}</button>)}</div>{history.status === 'loading' && <div className="flex min-h-[420px] items-center justify-center rounded-2xl border border-slate-200 bg-white"><span className="text-sm text-slate-500">Carregando resultados…</span></div>}{history.status === 'error' && <div role="alert" className="flex items-start gap-3 rounded-2xl border border-rose-200 bg-rose-50 p-5 text-sm text-rose-800"><AlertCircle size={18} /><div><p className="font-semibold">Os resultados locais não carregaram.</p><p className="mt-1">{history.error}</p><button type="button" onClick={() => history.load().catch(() => {})} className="mt-3 font-semibold underline">Tentar novamente</button></div></div>}{history.status === 'ready' && filtered.length === 0 && <div className="flex min-h-[420px] flex-col items-center justify-center rounded-2xl border border-dashed border-slate-300 bg-white px-6 text-center"><ImageIcon size={30} className="text-slate-400" /><h2 className="mt-4 text-base font-semibold text-slate-900">Nenhum resultado neste filtro</h2><p className="mt-2 text-sm text-slate-500">As gerações salvas localmente aparecerão aqui.</p></div>}{history.status === 'ready' && filtered.length > 0 && <div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map((result) => <ResultCard key={result.id} result={result} onOpen={() => history.open(result.id).catch(() => {})} />)}</div>}<ResultDetailModal result={history.selected} busy={history.mutationPending} onClose={history.close} onStatus={(status) => history.setReviewStatus(history.selected.id, status).catch(() => {})} onDelete={() => setDeleteTarget(history.selected)} />{deleteTarget && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/65 p-4"><div role="alertdialog" aria-modal="true" aria-labelledby="delete-result-title" className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-rose-50 text-rose-600"><AlertCircle size={19} /></div><h2 id="delete-result-title" className="mt-4 text-lg font-semibold text-slate-950">Excluir este resultado?</h2><p className="mt-2 text-sm leading-6 text-slate-500">A imagem, as referências preservadas e o metadata serão removidos somente deste computador.</p><div className="mt-6 flex justify-end gap-3"><button type="button" disabled={history.mutationPending} onClick={() => setDeleteTarget(null)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700">Cancelar</button><button type="button" disabled={history.mutationPending} onClick={() => history.remove(deleteTarget.id).then(() => setDeleteTarget(null)).catch(() => {})} className="rounded-xl bg-rose-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">Excluir</button></div></div></div>}</>;
}
