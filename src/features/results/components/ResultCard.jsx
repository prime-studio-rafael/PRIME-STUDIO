import { Clock3, ReceiptText } from 'lucide-react';
const STATUS = { pending: ['Aguardando aprovação', 'bg-amber-50 text-amber-700'], approved: ['Aprovado', 'bg-emerald-50 text-emerald-700'], rejected: ['Reprovado', 'bg-rose-50 text-rose-700'] };
export default function ResultCard({ result, onOpen }) {
  const [statusLabel, statusClass] = STATUS[result.reviewStatus] || STATUS.pending;
  return <article data-testid="result-card" className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_12px_36px_rgba(15,23,42,0.04)]"><button type="button" onClick={onOpen} className="block w-full text-left"><div className="aspect-square bg-slate-100"><img src={result.assets.result} alt="Resultado gerado" className="h-full w-full object-contain" /></div><div className="p-4"><div className="flex items-start justify-between gap-3"><p className="text-sm font-semibold text-slate-900">{result.templateLabel || result.templateId || 'Template não informado'}</p><span className={`shrink-0 rounded-full px-2 py-1 text-[10px] font-semibold ${statusClass}`}>{statusLabel}</span></div><p className="mt-1 text-xs text-slate-500">{formatDate(result.createdAt)}</p><p className="mt-3 truncate text-xs text-slate-500">{result.model || 'Modelo não informado'}</p><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-600"><span className="inline-flex items-center gap-1"><ReceiptText size={13} />{formatCost(result.costUsd)}</span><span className="inline-flex items-center gap-1"><Clock3 size={13} />{formatDuration(result.durationMs)}</span></div></div></button></article>;
}
export function formatDuration(value) { return Number.isFinite(value) ? value < 1000 ? `${value} ms` : `${(value / 1000).toFixed(1)} s` : 'Não informado'; }
export function formatCost(value) { return Number.isFinite(value) ? `US$ ${value.toFixed(4)}` : 'Não informado'; }
export function formatDate(value) { if (!value || Number.isNaN(Date.parse(value))) return 'Data não informada'; return new Intl.DateTimeFormat('pt-BR', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)); }
